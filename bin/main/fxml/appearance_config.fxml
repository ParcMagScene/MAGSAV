<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<ScrollPane xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.magsav.gui.AppearanceController">
   <content>
      <VBox spacing="20.0" styleClass="config-content">
         <padding>
            <Insets bottom="20.0" left="20.0" right="20.0" top="20.0" />
         </padding>
         
         <!-- Titre de la section -->
         <Label styleClass="section-title" text="Configuration de l'apparence" />
         
         <!-- Section Thème général -->
         <VBox spacing="15.0" styleClass="config-section">
            <Label styleClass="section-header" text="Thème général" />
            
            <HBox spacing="15.0" alignment="CENTER_LEFT">
               <Label text="Thème actuel :" minWidth="120" />
               <ComboBox fx:id="themeSelector" prefWidth="200" />
            </HBox>
         </VBox>
         
         <!-- Section Couleurs personnalisées -->
         <VBox spacing="15.0" styleClass="config-section">
            <Label styleClass="section-header" text="Couleurs personnalisées" />
            
            <!-- Couleur de la sidebar -->
            <HBox spacing="15.0" alignment="CENTER_LEFT">
               <Label text="Couleur sidebar :" minWidth="120" />
               <ColorPicker fx:id="sidebarColorPicker" prefWidth="100" />
               <Label fx:id="sidebarColorLabel" text="#1e3a5f" styleClass="color-value" />
            </HBox>
            
            <!-- Couleur de fond principal -->
            <HBox spacing="15.0" alignment="CENTER_LEFT">
               <Label text="Fond principal :" minWidth="120" />
               <ColorPicker fx:id="backgroundColorPicker" prefWidth="100" />
               <Label fx:id="backgroundColorLabel" text="#000000" styleClass="color-value" />
            </HBox>
            
            <!-- Couleur des onglets -->
            <HBox spacing="15.0" alignment="CENTER_LEFT">
               <Label text="Couleur onglets :" minWidth="120" />
               <ColorPicker fx:id="tabColorPicker" prefWidth="100" />
               <Label fx:id="tabColorLabel" text="#1a1a1a" styleClass="color-value" />
            </HBox>
            
            <!-- Couleur accent -->
            <HBox spacing="15.0" alignment="CENTER_LEFT">
               <Label text="Couleur accent :" minWidth="120" />
               <ColorPicker fx:id="accentColorPicker" prefWidth="100" />
               <Label fx:id="accentColorLabel" text="#4a90e2" styleClass="color-value" />
            </HBox>
            
            <!-- Couleur du texte -->
            <HBox spacing="15.0" alignment="CENTER_LEFT">
               <Label text="Couleur texte :" minWidth="120" />
               <ColorPicker fx:id="textColorPicker" prefWidth="100" />
               <Label fx:id="textColorLabel" text="#ffffff" styleClass="color-value" />
            </HBox>
         </VBox>
         
         <!-- Section Prévisualisation -->
         <VBox spacing="15.0" styleClass="config-section">
            <Label styleClass="section-header" text="Prévisualisation" />
            
            <VBox fx:id="previewPane" spacing="10.0" styleClass="theme-preview" maxWidth="400">
               <HBox styleClass="preview-sidebar" spacing="10.0" alignment="CENTER_LEFT">
                  <Label text="🏠" styleClass="preview-icon" />
                  <Label text="Dashboard" styleClass="preview-text" />
               </HBox>
               <HBox styleClass="preview-content" spacing="10.0">
                  <VBox styleClass="preview-tab" spacing="5.0" HBox.hgrow="ALWAYS">
                     <Label text="Contenu principal" styleClass="preview-title" />
                     <Label text="Texte d'exemple" styleClass="preview-text" />
                  </VBox>
               </HBox>
            </VBox>
         </VBox>
         
         <!-- Boutons d'action -->
         <HBox spacing="15.0" alignment="CENTER_LEFT">
            <Button fx:id="applyButton" text="Appliquer les changements" styleClass="primary-button" />
            <Button fx:id="resetButton" text="Restaurer par défaut" styleClass="secondary-button" />
            <Button fx:id="savePresetButton" text="Sauvegarder comme preset" styleClass="secondary-button" />
         </HBox>
         
         <!-- Section Presets sauvegardés -->
         <VBox spacing="15.0" styleClass="config-section">
            <Label styleClass="section-header" text="Presets sauvegardés" />
            
            <ListView fx:id="presetsList" prefHeight="120" styleClass="presets-list" />
            
            <HBox spacing="10.0">
               <Button fx:id="loadPresetButton" text="Charger preset" styleClass="secondary-button" />
               <Button fx:id="deletePresetButton" text="Supprimer preset" styleClass="danger-button" />
            </HBox>
         </VBox>
         
      </VBox>
   </content>
</ScrollPane>