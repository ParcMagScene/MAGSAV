<ToolBar>
  <!-- ...existing buttons... -->
</ToolBar>
<TableView fx:id="table" /* ... */>
  <contextMenu>
    <ContextMenu>
      <MenuItem text="Ajouter une sous-catégorie…" onAction="#onAddSubcategoryForSelected"/>
    </ContextMenu>
  </contextMenu>
  <!-- ...existing columns... -->
</TableView>